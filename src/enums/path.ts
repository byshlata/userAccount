export enum Path {
  Root = '/',
  Delete = '/delete',
  Register = '/register',
  Login = '/login',
  Auth = '/auth',
  Update = '/update',
  CreatePDF = '/create-pdf',
  UploadCloud = '/upload-cloud',
  Other = '*',
  User = '/user',
}
